import 'package:flutter/material.dart';

class Flights extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      //flights dummy page
      home: Center(child: Text('Flights')),
    );
  }
}
